language: node_js
cache:
  directories:
    - ~/.npm
notifications:
  email: false
node_js:
  - '8'
after_success:
  - npm install -g travis-deploy-once@4
  - travis-deploy-once "npm run semantic-release"
script:
  - npm run build
  - npm run lint
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
